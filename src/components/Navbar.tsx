// src/components/Navbar.tsx
import NavbarClient from "./NavbarClient";

export default function Navbar() {
  // All configuration (phone, IDX link, builders, etc.)
  // is now handled inside NavbarClient, so we just render it.
  return <NavbarClient />;
}
